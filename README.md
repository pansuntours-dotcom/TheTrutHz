# The TrutHz — starter

This is a starter Next.js (App Router) + TypeScript + Tailwind project with:
- Landing page
- Dynamic gallery (Supabase Storage `gallery` bucket)
- Live stream placeholder (HLS.js)

## Local dev

1. Install:
```bash
npm ci
